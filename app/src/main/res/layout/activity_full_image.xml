<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/darkDray"
    tools:context=".Activity.FullImageActivity">

    <LinearLayout
        android:id="@+id/linClose"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/imgClose"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:layout_margin="10dp"
            android:src="@drawable/ic_baseline_close_24"
            android:tint="@color/white" />

    </LinearLayout>

    <com.github.chrisbanes.photoview.PhotoView
        android:id="@+id/imgPhotoView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/txtNewsTitle"
        android:layout_below="@+id/linClose"
        android:transitionName="simple_activity_transition"></com.github.chrisbanes.photoview.PhotoView>

    <TextView
        android:id="@+id/txtNewsTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_margin="5dp"
        android:fontFamily="@font/cerebrisans_regular"
        android:text="dshcushcuh  jiosdjvio jijcids "
        android:textColor="@color/white"
        android:textSize="@dimen/text_font_news_title"></TextView>
</RelativeLayout>